//- views/layout.pug
doctype html
html
  head
    title= title || 'Мой блог'
    link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css')
    link(rel='stylesheet' href='/styles.css')
  body
    nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
      .container
        a.navbar-brand(href='/') Мой блог
        .navbar-nav
          if user
            a.nav-link(href='/articles/new') Новая статья
            a.nav-link(href='/auth/logout') Выйти
          else
            a.nav-link(href='/auth/login') Войти
    
    .container.mt-4
      block content

    footer.bg-dark.text-white.mt-5.p-4.text-center
      p © 2025 Мой блог